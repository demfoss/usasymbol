@model USASymbol.Models.ViewModels.SymbolDetailViewModel

@{
    ViewData["Title"] = $"{Model.Symbol.Name} - {Model.State.Name} State Bird";
}

<!-- Schema.org Structured Data -->
<script type="application/ld+json">
    {
      "@@context": "https://schema.org",
      "@@type": "Article",
      "headline": "@Model.State.Name State Bird - @Model.Symbol.Name",
      "description": "Complete guide to @Model.State.Name's official state bird, the @Model.Symbol.Name",
      "image": "@(Model.Symbol.ImageUrl ?? "/images/default-bird.jpg")",
      "datePublished": "@(Model.Symbol.AdoptedYear.HasValue ? new DateTime(Model.Symbol.AdoptedYear.Value, 1, 1).ToString("yyyy-MM-dd") : "2020-01-01")",
      "dateModified": "@DateTime.Now.ToString("yyyy-MM-dd")",
      "author": {
        "@@type": "Organization",
        "name": "USA Symbol"
      },
      "publisher": {
        "@@type": "Organization",
        "name": "USA Symbol",
        "logo": {
          "@@type": "ImageObject",
          "url": "@(Context.Request.Scheme)://@(Context.Request.Host)/logo.png"
        }
      },
      "mainEntityOfPage": {
        "@@type": "WebPage",
        "@@id": "@(Context.Request.Scheme)://@(Context.Request.Host)/states/@Model.State.Slug/bird"
      }
    }
</script>

<!-- BREADCRUMBS -->
<nav class="max-w-6xl mx-auto px-4 py-4 text-sm text-gray-600">
    <a href="/" class="hover:text-blue-900 transition">Home</a> /
    <a href="/symbols" class="hover:text-blue-900 transition">Symbols</a> /
    <a href="/symbols/birds" class="hover:text-blue-900 transition">State Birds</a> /
    <span class="text-gray-900 font-semibold">@Model.State.Name</span>
</nav>

<!-- MAIN CONTENT -->
<main class="max-w-6xl mx-auto px-4 pb-12">

    <!-- PAGE TITLE -->
    <div class="mb-6">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-2">
            @Model.State.Name State Bird: @Model.Symbol.Name
        </h1>
        @if (Model.Symbol.AdoptedYear.HasValue)
        {
            <p class="text-xl text-gray-600">A Symbol of Vitality and Resilience Since @Model.Symbol.AdoptedYear</p>
        }
        else
        {
            <p class="text-xl text-gray-600">Official State Bird of @Model.State.Name</p>
        }
    </div>

    <!-- AUTHOR BOX -->
    <div class="bg-blue-50 rounded-lg p-4 border-l-4 border-blue-600 mb-8 shadow-sm">
        <div class="flex items-center justify-between flex-wrap gap-4">
            <div class="flex items-center gap-3">
                <div class="w-12 h-12 rounded-lg border-2 border-blue-200 bg-gradient-to-br from-blue-900 to-blue-600 flex items-center justify-center text-white font-bold text-xl">
                    US
                </div>
                <div>
                    <div class="flex items-center gap-2">
                        <span class="text-sm font-semibold text-gray-800">Researched by USA Symbol Team</span>
                        <svg class="w-4 h-4 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                        </svg>
                    </div>
                    <p class="text-xs text-gray-500">Fact-checked • Updated @DateTime.Now.ToString("MMMM d, yyyy")</p>
                </div>
            </div>
            <div class="flex items-center gap-3 text-xs">
                <a asp-controller="Home" asp-action="About" class="text-blue-700 hover:text-blue-900 font-semibold transition">About Editor</a>
            </div>
        </div>
    </div>

    <!-- HERO SECTION -->
    <section class="mb-10">
        <div class="bg-white border rounded-2xl overflow-hidden shadow-lg">
            <div class="md:flex">
                <!-- Bird Image -->
                <div class="w-full md:w-1/2 h-64 md:h-96 bg-gradient-to-br from-blue-100 to-indigo-200 flex items-center justify-center">
                    @if (!string.IsNullOrEmpty(Model.Symbol.ImageUrl))
                    {
                        <img src="@Model.Symbol.ImageUrl" alt="@Model.Symbol.Name" class="w-full h-full object-cover">
                    }
                    else
                    {
                        <div class="text-center p-8">
                            <div class="text-8xl mb-4">🦅</div>
                            <p class="text-sm text-gray-500">@Model.Symbol.Name</p>
                        </div>
                    }
                </div>

                <!-- Bird Info -->
                <div class="p-6 md:p-8 flex-1">
                    <div class="inline-block bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-xs font-semibold mb-4">
                        @Model.State.Name State Bird
                    </div>

                    <h2 class="text-3xl font-bold text-gray-900 mb-4">@Model.Symbol.Name</h2>

                    @if (!string.IsNullOrEmpty(Model.Symbol.ScientificName))
                    {
                        <p class="text-lg text-gray-600 italic mb-6">@Model.Symbol.ScientificName</p>
                    }

                    <div class="space-y-3">
                        <div class="flex items-start gap-2">
                            <svg class="w-5 h-5 text-blue-700 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                            <div>
                                <span class="font-semibold text-gray-900">State:</span>
                                <a href="/states/@Model.State.Slug" class="text-blue-700 hover:underline ml-1">@Model.State.Name</a>
                            </div>
                        </div>

                        @if (Model.Symbol.AdoptedYear.HasValue)
                        {
                            <div class="flex items-start gap-2">
                                <svg class="w-5 h-5 text-blue-700 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                                </svg>
                                <div>
                                    <span class="font-semibold text-gray-900">Adopted:</span>
                                    <span class="ml-1">@Model.Symbol.AdoptedYear</span>
                                </div>
                            </div>
                        }

                        @if (!string.IsNullOrEmpty(Model.Symbol.ScientificName))
                        {
                            <div class="flex items-start gap-2">
                                <svg class="w-5 h-5 text-blue-700 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                                </svg>
                                <div>
                                    <span class="font-semibold text-gray-900">Scientific Name:</span>
                                    <span class="ml-1 italic">@Model.Symbol.ScientificName</span>
                                </div>
                            </div>
                        }
                    </div>

                    <div class="mt-6 pt-6 border-t">
                        <a href="/symbols/birds" class="inline-flex items-center text-blue-700 font-semibold hover:text-blue-900 transition">
                            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                            </svg>
                            View All State Birds
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- MARKDOWN CONTENT OR DEFAULT CONTENT -->
    @if (Model.Content != null && !string.IsNullOrEmpty(Model.Content.Html))
    {
        <!-- Если есть markdown контент -->
        <section class="prose prose-lg max-w-none mb-10">
            <div class="bg-white rounded-xl p-8 shadow-sm">
                @Html.Raw(Model.Content.Html)
            </div>
        </section>
    }
    else
    {
        <!-- Placeholder контент если нет markdown -->
        <section class="bg-white rounded-xl p-8 shadow-sm mb-10">
            <h2 class="text-2xl font-bold mb-6 text-gray-900">About the @Model.Symbol.Name</h2>

            <div class="prose prose-lg max-w-none">
                <p class="text-gray-700 leading-relaxed mb-4">
                    The <strong>@Model.Symbol.Name</strong>
                    @if (!string.IsNullOrEmpty(Model.Symbol.ScientificName))
                    {
                        <text>(<em>@Model.Symbol.ScientificName</em>)</text>
                    }
                    is the official state bird of @Model.State.Name.
                    @if (Model.Symbol.AdoptedYear.HasValue)
                    {
                        <text>This designation was officially adopted in @Model.Symbol.AdoptedYear, making it a cherished symbol of the state's natural heritage.</text>
                    }
                </p>

                <h3 class="text-xl font-bold mt-8 mb-4 text-gray-900">Why This Bird Represents @Model.State.Name</h3>
                <p class="text-gray-700 leading-relaxed mb-4">
                    State birds are chosen to represent the unique character and natural beauty of each state.
                    The @Model.Symbol.Name was selected as @Model.State.Name's official bird due to its
                    significance to the state's ecosystem and cultural identity.
                </p>

                <h3 class="text-xl font-bold mt-8 mb-4 text-gray-900">Interesting Facts</h3>
                <ul class="list-disc pl-6 space-y-2 text-gray-700">
                    <li>The @Model.Symbol.Name is one of the most recognizable birds in @Model.State.Name</li>
                    @if (Model.Symbol.AdoptedYear.HasValue)
                    {
                        <li>This bird has been @Model.State.Name's official state symbol for over @(DateTime.Now.Year - Model.Symbol.AdoptedYear.Value) years</li>
                    }
                    <li>State birds play an important role in environmental education and conservation efforts</li>
                </ul>
            </div>
        </section>
    }

    <!-- OTHER STATE SYMBOLS -->
    @if (Model.RelatedSymbols.Any())
    {
        <section class="mb-10">
            <h2 class="text-2xl font-bold mb-6 text-gray-900">Other @Model.State.Name State Symbols</h2>
            <div class="grid md:grid-cols-3 gap-6">
                @foreach (var symbol in Model.RelatedSymbols)
                {
                    <a href="/states/@Model.State.Slug/@symbol.Type"
                       class="bg-white rounded-xl p-6 shadow-sm hover:shadow-lg transition border-2 border-transparent hover:border-blue-500 smooth-transition">
                        <div class="text-5xl mb-4 text-center">@GetSymbolIcon(symbol.Type)</div>
                        <div class="text-center">
                            <div class="text-sm text-blue-700 font-semibold uppercase mb-2">@GetSymbolTypeName(symbol.Type)</div>
                            <h3 class="text-lg font-bold text-gray-800 mb-1">@symbol.Name</h3>
                            @if (!string.IsNullOrEmpty(symbol.ScientificName))
                            {
                                <p class="text-sm text-gray-500 italic">@symbol.ScientificName</p>
                            }
                        </div>
                    </a>
                }
            </div>
        </section>
    }

    <!-- NAVIGATION LINKS -->
    <div class="mt-10 flex flex-wrap gap-4 justify-center">
        <a href="/symbols/birds"
           class="px-6 py-3 rounded-lg bg-white border-2 border-blue-600 text-blue-600 font-semibold hover:bg-blue-50 transition shadow-sm">
            ← Back to All State Birds
        </a>
        <a href="/states/@Model.State.Slug"
           class="px-6 py-3 rounded-lg bg-blue-600 text-white font-semibold hover:bg-blue-700 transition shadow-sm">
            See All @Model.State.Name Symbols →
        </a>
    </div>

</main>

@functions {
    string GetSymbolTypeName(string type)
    {
        return type switch
        {
            "bird" => "State Bird",
            "flower" => "State Flower",
            "tree" => "State Tree",
            "motto" => "State Motto",
            "animal" => "State Animal",
            "flag" => "State Flag",
            _ => $"State {char.ToUpper(type[0]) + type.Substring(1)}"
        };
    }

    string GetSymbolIcon(string type)
    {
        return type switch
        {
            "bird" => "🦅",
            "flower" => "🌸",
            "tree" => "🌳",
            "motto" => "📜",
            "animal" => "🦌",
            "flag" => "🏴",
            _ => "⭐"
        };
    }
}