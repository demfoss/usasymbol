# robots.txt for usasymbol.com

User-agent: *
Allow: /

# PDF endpoints send X-Robots-Tag: noindex and old direct links redirect to the page.
# Keep crawl access open so search engines can see those signals and drop stale URLs.

# Sitemap location
Sitemap: https://usasymbol.com/sitemap.xml

# Common bot protection (optional - uncomment if needed)
# Disallow: /api/
# Disallow: /admin/
# Disallow: /_next/
# Disallow: /search

# Crawl delay (optional - uncomment if server gets overloaded)
# Crawl-delay: 1
